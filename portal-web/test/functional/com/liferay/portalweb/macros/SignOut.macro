definition {

	@summary = "Sign out of Liferay Portal"
	macro signOut {
		Navigator.openURL();

		UserBar.gotoDropdownItem(dropdownItem = "Sign Out");
	}

	macro signOutOfSpecificURL {
		Navigator.openSpecificURL(url = "${url}");

		UserBar.gotoDropdownItem(dropdownItem = "Sign Out");
	}

}